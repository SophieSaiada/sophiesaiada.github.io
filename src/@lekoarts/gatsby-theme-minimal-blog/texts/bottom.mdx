import { Link } from "gatsby";
import { Flex, Heading } from "theme-ui";
import CTA from "../../../components/cta";
import emailIcon from "../../../images/icons/email-24px.svg";
import downloadIcon from "../../../images/icons/download-24px.svg";

<Flex
  sx={{
    mb: 5,
    flexDirection: ["column", "column", "row"],
    justifyContent: "space-between"
  }}
>
    <Text sx={{ fontSize: [2, 2, 3], textAlign: "center", mt: 3  }}>Let's code a better world together.</Text>
    <CTA text="Contact Me" icon={emailIcon} to="/contact/" sx={{ mt: 3 }} gatsbyLink />
</Flex>
